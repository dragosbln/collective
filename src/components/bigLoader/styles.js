import { createStyles } from "@material-ui/core";

const styles = theme => createStyles({
    container: {
        marginTop: 100,
        maxWidth: 300,
        marginRight: 'auto',
        marginLeft: 'auto',
    },
    logo: {
        width: '100%'
    }
});

export default styles;
